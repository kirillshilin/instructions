Use regular `scaf` commands in this workspace.

Run `scaf list` and choose the closest template.

Example with a bundled template:

```bash
scaf node-cli my-cli --var name=my-cli --yes
```

If no template fits, continue with the manual steps below.